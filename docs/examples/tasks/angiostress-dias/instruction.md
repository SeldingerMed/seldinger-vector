# AngioStress contract (seed)

You are a frozen perception model. Emit the metrics named by this task's prediction schema against the labelled items.

The claim boundary travels with the contract. A result without that footer is invalid. This seed does not ship DIAS pixels; it scores submitted metric JSON the same way a full AngioStress run will.
